name: find_your_medication
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  mobx: ^1.2.1+1
  flutter_mobx: ^1.1.0+1
  provider: ^4.3.1
  http: ^0.12.2
  fluttertoast: ^4.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: "^0.7.3"

  mobx_codegen: ^1.1.0+1
  build_runner: ^1.10.0

flutter_icons:
  android: "ic_launcher"
  ios: true
  image_path: "images/ic_launcher.jpg"

flutter:

  uses-material-design: true

  assets:
    - images/

